import axiosInstance from './axiosConfig';

/**
 * Export the configured axiosInstance with authentication interceptors
 * This is used by ApiService and can be used directly if needed
 */
export default axiosInstance;
