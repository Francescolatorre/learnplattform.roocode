{
  "name": "learnplatform-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "playwright:mcp": "npx @playwright/test test --config=playwright-mcp.config.js"
  },
  "devDependencies": {
    "@playwright/mcp": "^0.0.26"
  }
}
